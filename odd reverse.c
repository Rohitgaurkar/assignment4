#include<stdio.h>
#include<conio.h>
int main()
{
    int i=10;
    while(i<=10)
    {
        if(i!=0)
        {
            printf("%d ",2*i-1);
            i--;
        }
    }

    return 0;
}
